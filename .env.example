# PetalLink 本地开发环境配置
#
# 使用方式：
#   cp .env.example .env
#   然后填入真实的 client_id 和 client_secret
#
# -------------------------------------------------------------------
# 如何获取 client_id 和 client_secret
# -------------------------------------------------------------------
#
# 1. 登录华为 AppGallery Connect 控制台：
#    https://developer.huawei.com/consumer/cn/service/josp/agc/index.html
#
# 2. 创建项目 → 添加应用，选择"Web 应用"。
#
# 3. 进入「认证服务」→「OAuth 2.0 客户端配置」，创建客户端：
#    - 授权类型勾选"授权码"（authorization_code）
#    - 回调地址填：http://127.0.0.1:9999/oauth/callback
#    - API 管理 需要开通“云空间”（否则登录报 1101 invalid scope）：
#
# 4. 创建完成后，页面会显示 client_id 和 client_secret，填入下方。
#
HWCLOUD_CLIENT_ID=
HWCLOUD_CLIENT_SECRET=
