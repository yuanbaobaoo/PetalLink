# 13 · 发布与兼容验收

## 1. 发布输入

- 唯一版本：`gradle.properties` 的 `petalLinkVersion`
- bundle id：`io.github.yuanbaobaoo.PetalLink`（沿用原 Tauri 应用）
- 最低系统：macOS 12.0
- 架构：当前发布为 arm64
- 更新 endpoint：`BuildInfo.UPDATE_ENDPOINT`
- 正式构建必须注入 `PETALLINK_UPDATE_TEAM_ID`，否则客户端 fail closed 拒绝安装更新

## 2. GitHub Secrets

- `APPLE_DEVELOPER_ID_APPLICATION_P12_BASE64`
- `APPLE_DEVELOPER_ID_APPLICATION_P12_PASSWORD`
- `APPLE_SIGNING_IDENTITY`
- `APPLE_TEAM_ID`
- `APPLE_NOTARY_ID`
- `APPLE_NOTARY_PASSWORD`
- `HWCLOUD_CLIENT_ID`
- `HWCLOUD_CLIENT_SECRET`

## 3. 自动验收

客户端更新检查保留原版节奏：启动 3 秒后静默检查、每小时检查、窗口聚焦按 10 分钟节流，设置页手动检查不受节流；发现新版本后在主界面和设置页提供入口。

CI 执行以下门禁：

1. `:shared:jvmTest`：单元、HTTP 合同、临时目录集成和 macOS 平台测试；当前 317 项通过。
2. `:shared:packageDmg`：实际生成无签名 DMG。
3. `scripts/verify-macos-artifacts.sh`：核对版本、bundle id、最低系统、图标、`java.sql` 运行时模块、JVM/JNA entitlement 和 codesign 结构。
4. `scripts/smoke-test-macos-app.sh`：用隔离数据目录启动打包后的应用，等待数据库/服务装配，再验证第二实例正常退出且主实例继续存活。
5. Release 额外执行 Developer ID 签名、notarytool、staple、Gatekeeper 校验。
6. `.app.zip` 生成 SHA-256 manifest；客户端安装前再次核验 SHA-256、codesign、spctl 与固定 Team ID。

## 4. 原 Tauri 数据兼容

CMP 继续使用原 bundle id，因此直接读取同一目录：

`~/Library/Application Support/io.github.yuanbaobaoo.PetalLink`

- `token.bin`：`PTL1 + nonce + ChaCha20-Poly1305` 完全沿用，明文 length-prefix 布局一致。
- `config.json`：字段与枚举字符串保持 camelCase 兼容。
- `petal_link.db`：首次打开按 v2/v3/v4/v5 → v6 原子升级；v6 直接复用。
- 云树/checkpoint：完整性不足或 cursor 失效时 fail closed 并安全重建，不要求删除用户文件。

对应自动测试：`LegacyTauriCompatibilityTest`、`TokenSerializerTest`、`JsonConfigStoreTest`、`PetalLinkDbTest`。

## 5. 原命令替代关系

- `drive_list_all`：`driveListAll` 保留；UI 浏览使用 cursor 分页避免大目录一次性载入。
- `drive_download_file`：UI 统一走 `syncDownloadOnDemand`，两者最终进入同一持久 TaskRunner。
- `sync_free_up_space`：多选和单选统一组装为 batch，复用同一 `JvmFreeUpService` 安全协议。
- `transfer_clear_completed/failed/finished`：底层保留三种精确语义，UI 用 finished 作为默认入口。
- `sync_folder_recursive`：保留“立即返回 0 + 后台执行”语义；在统一 owner 内 BFS 目标云端子树，下载真实内容、上传本地独有文件，完成后再全量核对 checkpoint。
- Tauri updater：替换为签名 `.app.zip` staging/helper；替换或启动失败自动恢复 `.backup`。

## 6. 发布后人工矩阵

以下项目依赖真实华为账号、Apple 凭据或系统 UI，必须在 Release Candidate DMG 上执行并记录版本/机器/结果：

- 新用户：安装 → 登录 → 选择空目录 → 首次索引 → 双向同步。
- 老用户：覆盖安装后恢复 token/config/DB，旧挂载目录不重建、不丢基线。
- 网络：离线启动、上传/下载中断、429/5xx、401 refresh、恢复网络。
- 文件：0B、>20MiB、中文、emoji、rename/move/copy、持续编辑和占位按需下载。
- 云端：rename/move/delete、cursor 400/410、空中间页、完整空盘。
- 生命周期：Close、Cmd+Q、Dock Quit、系统关机/登出、LaunchAgent、第二实例。
- 发布：DMG Gatekeeper、签名、notarization/staple、更新成功、替换失败回滚。

人工矩阵未签字前，只能称为“构建通过”，不能称为“生产验收通过”。

## 7. 当前无签名制品记录（2026-07-16）

- DMG：`shared/build/compose/binaries/main/dmg/PetalLink-1.0.12.dmg`
- SHA-256：`868115fb1fb4dd2fd69a64317b8707cfbb374e131c68dfc2d86af6ada23d1ae1`
- 已通过：版本/bundle id/最低系统/图标/`java.sql`/entitlements/codesign 结构门禁
- 已通过：最新构建目录 `.app` 的隐藏启动、服务装配、第二实例退出/主实例存活冒烟
- 待重复：最新 SHA-256 对应 DMG 的“只读挂载 → `ditto` 复制到隔离安装目录 → 复制品冒烟”；该流程已在前一同版本构建通过
- 未通过亦未声称通过：Developer ID、notarization/staple、Gatekeeper 正式链、真实华为账号和系统 UI 人工矩阵
